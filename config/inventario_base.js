/**
 * Inventario Base por Defecto
 * Este arreglo sirve únicamente como fallback en caso de que state.json no exista.
 * Los datos reales se leen dinámicamente de state.json en la raíz del proyecto.
 */

const inventarioInicial = [];

module.exports = inventarioInicial;
